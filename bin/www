#!/usr/bin/env node

const http = require('http')
  , app = require('../')
  , log = require('../lib/log')
  , port = process.env.PORT || 3000

http.globalAgent.maxSockets = Infinity
http.createServer(app.callback())

app.listen(port)

log.info(`Event Track API - port ${port}`)
